#set( $CAMEL_NAME = ${StringUtils.removeAndHump(${NAME})})
import 'package:liquidart/liquidart.dart';
import 'package:${PROJECT_NAME}/${PROJECT_NAME}.dart';

class ${CAMEL_NAME} extends HTTPController {
  
}
